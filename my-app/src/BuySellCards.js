import React, { useState, useEffect } from "react";


function BuySellCards({ card, moneyState }) {
    

    // if (card.price <= moneyState) {
    //     // deduct card price from moneyState 
    //     // then add card to collection
    // }

  return (
    
    <div className="card"></div>
  );
}

export default BuySellCards;